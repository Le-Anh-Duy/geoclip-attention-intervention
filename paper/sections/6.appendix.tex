\section*{Appendix: Supplementary Material}

\begin{figure}[h]
  \centering
  \placeholderfig{32mm}{figures/qualitative.png --- three or four examples:
  image with the proposed region, unmodified and intervened predictions, and
  the resulting displacement in km}
  \caption{Qualitative examples. \TODO{describe each case}}
  \label{fig:qualitative}
  \vspace{-3mm}
\end{figure}

\input{tables/tab_effect}

\subsubsection*{Demonstration video.}
\TODO{URL} (\TODO{duration}, within the three-minute limit), presenting the
three scenarios of Section~\ref{sec:demo} end to end on a live instance.

\subsubsection*{Source code and reproduction.}
\TODO{repository URL}, containing the FastAPI backend, the React frontend,
the detection pipeline and the evaluation scripts that regenerate
Tables~\ref{tab:main} and~\ref{tab:effect} from the released per-image
predictions. \TODO{exact commands and wall-clock runtime}

\subsubsection*{Models, data and hardware.}
GeoCLIP with a CLIP ViT-L/14 vision tower and the released $10^{5}$-point GPS
gallery; region proposals at an objectness threshold of $0.4$; Img2GPS3k
($2{,}997$ images). No component is fine-tuned or retrained.
\TODO{demonstration laptop GPU and memory, CPU-only fallback, per-request
latency}
