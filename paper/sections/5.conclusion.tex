\section{Conclusion}

We have presented GeoProbe, an interactive system that renders the question
of what a geolocation model attends to amenable to experiment rather than to
inspection alone. By biasing pre-softmax attention toward or away from an
open-vocabulary detected region, independently per layer and at inference
time, the system converts an arbitrary photograph into a causal analysis
whose outcome is read from the displacement of the predicted coordinate. The
same mechanism supports both interactive exploration and systematic
measurement \TODO{restate the principal quantitative finding}. Ongoing work
extends the system to representation-level intervention and to searched
vocabularies of geographic cues, so that the concepts analysed are
discovered rather than specified in advance.
