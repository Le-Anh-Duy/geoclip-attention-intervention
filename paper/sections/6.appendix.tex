\section*{Appendix: Supplementary Material}

\subsubsection*{Per-group ablation.}
Table~\ref{tab:groups} isolates the contribution of each layer group at the
best searched magnitude, by zeroing the other two parameters.

\begin{table}[h]
\centering
\caption{Effect of restricting the intervention to a single layer group.
\TODO{populate; requires three additional evaluations beyond the joint
search}}
\label{tab:groups}
\setlength{\tabcolsep}{5pt}
\begin{tabular}{L{3.4cm}|P{1.4cm}P{1.4cm}P{1.4cm}|P{1.7cm}P{1.7cm}}
\toprule
\textbf{Active group} & $\delta_1$ & $\delta_2$ & $\delta_3$ &
\textbf{Mean (km)} & \textbf{$\Delta$ base} \\
\midrule
None (baseline) & 0 & 0 & 0 & -- & --- \\
Early ($0$--$7$) & -- & 0 & 0 & -- & -- \\
Middle ($8$--$15$) & 0 & -- & 0 & -- & -- \\
Late ($16$--$23$) & 0 & 0 & -- & -- & -- \\
All three & -- & -- & -- & -- & -- \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection*{Demonstration video.}
\TODO{URL} (\TODO{duration}, within the three-minute limit). The video
presents the three scenarios of Section~\ref{sec:demo} end to end on a live
instance of the system.

\subsubsection*{Source code.}
\TODO{repository URL}. The repository contains the FastAPI backend, the
React frontend, the Grounding DINO preparation notebook that produces the
detection manifest for Img2GPS3k, and the search notebook that reproduces
Table~\ref{tab:main}.

\subsubsection*{Models and data.}
GeoCLIP with a CLIP ViT-L/14 vision tower and the released $10^{5}$-point
GPS gallery; Grounding DINO
(\texttt{IDEA-Research/grounding-dino-base}) with box and text thresholds of
$0.4$ and $0.3$; and Img2GPS3k ($2{,}997$ images). No component is
fine-tuned or retrained.

\subsubsection*{Reproducing the quantitative study.}
\TODO{exact commands, seed $42$, and the wall-clock runtime of the complete
search on the hardware used}

\subsubsection*{Hardware for the live demonstration.}
\TODO{laptop GPU and memory, whether a CPU-only fallback is supported, and
the per-request latency in each configuration}
